<?xml version="1.0" encoding="UTF-8"?>
<project name="FreedomHostingMod" default="default" basedir=".">
    <description>Builds, tests, and runs the project FreedomHostingMod.</description>
    <import file="nbproject/build-impl.xml"/>

    <target name="-post-jar">
        <delete file="${dist.dir}/README.TXT" />
        <delete dir="${dist.dir}/lib/" />
    </target>
    
</project>
